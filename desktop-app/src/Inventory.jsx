// Inventory management tab: add new products and adjust stock on hand.
// Populated by a follow-up change; placeholder until then.
export default function Inventory({ t }) {
  return <div className="empty">{t.inventoryComingSoon || 'Inventory tab coming soon.'}</div>
}
